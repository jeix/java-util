# opencode 글로벌 AGENTS 설정

## 응답 스타일
- 한국어 해요체로 응답
- 코드·식별자·명령어·로그·파일 경로는 원문 그대로 유지

## Git 커밋 정책
- 작업 단위가 완결되어 커밋할 때, 커밋 전에 사용자에게 확인 요청
- 커밋 메시지는 한글로 작성
- 푸시는 사용자가 명시적으로 요청할 때만 수행

## 세션 연속성 및 문서화
단일 세션에서 모든 작업이 완료되지 않을 수 있으므로, 후속 세션을 위한 지속 문서를 유지합니다.

### 유지해야 할 문서 파일
- **AGENT**: 작업 시작 시점의 역할 정의 및 시작 지시사항
- **PRD**: 프로젝트 요구사항 정의서
- **PLAN**: 세부 실행 계획 및 마일스톤
- **DECISION**: 중요한 결정 사항 및 그 근거

### 문서화 목적
- 프로젝트의 그라운드 룰 명시
- 무엇을 만들지에 대한 명확한 기준 제공
- 계획과 결과, 수행할 다음 단계 투명하게 기록
- 결정 내용과 근거를 남겨 재해 복구 및 이해 촉진

## 작업 수행 규칙

코드를 작성하기 전에 아래를 순서대로 따른다. 계획 승인 전에는 어떤 파일도 수정하지 않는다.

### 1. 계획·아키텍처
- 텍스트로 계획을 제시한다: 무엇을(What), 왜(Why), 어떤 순서로(How), 그리고 리스크.
- 전체 구조(모듈 경계, 데이터 흐름, 주요 인터페이스)와 그 선택 이유를 함께 밝힌다.
- 승인 후에는 계획·아키텍처를 임의로 바꾸지 않는다. 변경은 먼저 알리고 승인받는다.

### 2. 태스크 분해
- 한 세션/커밋에 끝나는 크기로 쪼개고, 각 단계에 입력/출력, 수정할 파일 목록, 완료 기준(예/아니오 판정)을 명시한다.
- 기본은 **기능 단위**(눈으로 확인 가능한 동작 결과물)로 분할한다.
- 공통 스키마·타입·인증처럼 모두가 의존하는 기반은 0단계로 **레이어 단위** 확정한다.
- 명시된 파일 목록 밖은 건드리지 않는다.

### 3. 실행 순서
- 의존성이 없다고 명시적으로 확인되지 않은 한 단계는 하나씩 순차 진행한다.
- 병렬 가능 여부가 불확실하면 스스로 판단하지 않고 순차로 간다.

### 4. 테스트·확인
- 각 단계마다 정상 1개 이상, 경계/실패 1개 이상 테스트를 작성해 직접 실행하고 로그를 제시한다.
- 기존 테스트 스위트 통과(회귀)도 함께 확인한다.
- 단계 종료 시 사람이 직접 실행/확인 가능한 상태로 만든다. 문제는 다음 단계로 넘기지 않고 그 자리에서 고친다.

### 5. 막히면 멈춘다
- 에러·애매한 상황이면 우회하거나 스코프를 바꾸지 않고 즉시 멈춰 상황과 선택지를 보고한 뒤 지시를 기다린다.
- 완료 기준을 맞추려고 하드코딩, 조건 우회, 목업으로 대체하지 않는다.

### 6. 기록
- 단계 종료 시 한 일과 남은 일을 문서(설계 문서·체크리스트·커밋 메시지)에 남긴다. 대화창에만 남기지 않는다.
- 전체 계획은 체크리스트로 관리하고 완료 시 갱신한다.

### 7. 컨텍스트 관리
- 다음 신호가 보이면 즉시 알린다: 이미 결정한 내용을 다시 묻기, 용어 불일치, 정정된 사실이 문서에 틀린 채 남음.
- 위 신호가 나오거나 한 단계가 끝나면 새 세션 전환을 먼저 제안하고, 인계 내용(진행 상황·남은 작업·결정 사항)을 문서에 정리한다.

## 프로젝트별 설정

### 빌드 도구
- 시스템에 Maven이 설치되어 있지 않을 경우, **Maven Wrapper**(`/mvnw`)를 사용한다.
- Maven Wrapper JAR은 `.mvn/wrapper/maven-wrapper.jar`에 포함하며, `.gitignore`에서 제외하지 않는다.

### 의존성
- Lombok: `provided` 스코프 (컴파일 타임에만 필요)
- Jackson: `compile` 스코프 (런타임에 필요)
- JUnit Jupiter: `test` 스코프 (테스트에만 필요)

### 브랜치 전략
- `main`은 원격에 존재하는 공유 브랜치
- 기능 브랜치(`opencode-kilo` 등)에서 작업하며, 완료 시 사용자 확인 후 `main`으로 병합
- 각 브랜치의 파일은 서로 격리되어 있으며, 다른 브랜치의 파일이 체크아웃 시 혼선을 일으킬 수 있음

### 코드 스타일
- Java: Lombok `@UtilityClass`로 유틸리틸리티 클래스 작성
- 테스트: JUnit 5 (Jupiter) 사용
- 패키지명: 소문자 단일 단어 (예: `s`)
