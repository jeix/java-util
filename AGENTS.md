# AGENTS.md - 프로젝트 작업 규칙

## 프로젝트 개요

`java-util`은 자바 유틸리티 클래스와 그 테스트를 쌓아가는 저장소입니다.
루트 패키지는 `s`를 사용하고, 유틸리티는 `s` 또는 `s.util` 같은 하위 패키지에 둡니다.

## 빌드와 검증

- JDK 21을 기준으로 컴파일합니다(`maven.compiler.release=21`).
- 빌드 도구는 커밋된 Maven Wrapper를 사용합니다. 시스템 `mvn`이나 `gradle` 설치를 전제로 하지 않습니다.
- 테스트는 JUnit 5(Jupiter)로 작성하고, 클래스 이름은 `<대상클래스>Test`로 맞춥니다.
- 오프라인 환경에서는 `./mvnw -o test`처럼 로컬 캐시만 사용합니다.

## 작업 흐름

- 커밋 전 변경 범위에 맞는 검증(최소 `./mvnw test`)을 실행합니다.
- 커밋 메시지는 간결한 한글로 작성합니다.
- push는 명시적으로 요청받은 경우에만 실행합니다.
- secret, 빌드 산출물(`target/`), 임시 파일, 다른 브랜치에 속한 파일은 커밋하지 않습니다.

## 문서

- `PRD.md`: 목표, 범위, 요구사항
- `PLAN.md`: 계획, 수행 결과, 현재 상태와 다음 단계
- `DECISION.md`: 주요 결정, 검토한 대안과 근거
- 의미 있는 진행이나 결정이 생기면 위 문서를 같은 작업 단위에서 갱신합니다.
