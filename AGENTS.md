# 작업 지침

## 소통과 구현

- 사용자에게 한국어 해요체로 응답해요. 코드, 식별자, 명령어, 로그, 파일 경로는 원문을 유지해요.
- 메서드는 먼저 파라미터를 검사하고 정상 처리할 수 없으면 조기 반환한 뒤 정상 케이스를 처리해요.
- 필요한 기능이 기존 메서드에 있으면 재사용해요. 오버로드는 가능한 한 하나의 실질 구현에 위임해요.
- 유틸리티는 `src/main/java`, 동일 패키지의 JUnit 테스트는 `src/test/java`에 작성해요. 테스트 이름은 `*Test`로 해요.
- JDK 21과 저장소의 Maven Wrapper를 사용해요. 실행 명령과 API 동작은 [README.md](README.md)를 참고해요.

## 검증과 Git

- 사용자가 선택한 현재 브랜치에서 계속 작업해요.
- 커밋 전에 변경 범위에 맞는 안전한 검증을 실행해요. Java 변경은 `./mvnw clean verify`, 문서 변경은 내용·링크와 `git diff --check`를 확인해요. formatter/lint는 현재 별도 설정이 없으며 도입되면 해당 검사도 실행해요.
- 완결된 작업 단위의 커밋 시점에는 사용자에게 커밋 여부를 물어요. 해당 작업의 커밋을 명시적으로 요청받았으면 다시 묻지 않아요.
- 커밋 메시지는 간결한 한글로 작성해요. push는 사용자가 명시적으로 요청한 경우에만 실행해요.
- secret, 생성물, 임시 파일, 현재 작업과 무관한 사용자 변경은 커밋하지 않아요. `target/`은 생성물이에요.

## 세션 인계

- 중요한 진행이나 결정 뒤 관련 문서를 갱신하고 완료·대기 상태를 구분해요.
- `AGENTS.md`: 프로젝트 ground rule과 contributor instruction.
- `PRD.md`: 목표·범위·요구사항.
- `PLAN.md`: 계획·결과·현재 상태·다음 단계.
- `DECISION.md`: 주요 결정·대안·근거.
- `README.md`: 사용법과 현재 API 동작. 중복 설명은 링크로 연결하고 문서는 간결하게 유지해요.
