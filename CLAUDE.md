# java-util 프로젝트 규칙

이 저장소에서 코드를 쓰거나 고칠 때 지킬 것들. 각 규칙을 정한 이유는 `DECISIONS.md` 에 있다.

## 빌드·테스트

- `./mvnw test` 로 돌린다. `mvn` 은 설치돼 있지 않다.
- 커밋 전에는 `./mvnw clean test` 로 처음부터 돌려 확인한다.
- 컴파일 경고가 없어야 한다. `javac -Xlint:all` 로 새 소스를 한 번 돌려 본다.

## 메서드 작성

- **가드 먼저**: 파라미터를 검사해서 정상 처리할 수 없는 경우는 먼저 반환하고, 정상 케이스를
  마지막에 처리한다.
- **재사용**: 필요한 기능이 이미 다른 메서드에 있으면 그 메서드를 호출한다.
- **오버로드**: 실질적인 구현은 한 곳에 두고, 나머지 오버로드는 그 구현을 호출한다.
- **비공개 메서드 이름**: 앞에 `_` 를 붙인다. (`_clamp`, `_firstNonBlankOrLastByLoop` …)
- Javadoc 은 한국어로 쓰고, 클래스 첫 주석에 그 클래스가 지키는 규칙을 적는다.
  `null` 규칙과 인덱스 규칙은 특히 클래스마다 명시한다.

## 테스트

- JUnit 5. 테스트 클래스에 Lombok `@Slf4j` 를 붙인다.
- `@Nested` 로 묶고 `@DisplayName` 은 한국어로 쓴다. 케이스가 표로 정리되면
  `@ParameterizedTest` + `@CsvSource` 를 쓴다.

## 같은 일을 하는 구현이 둘 이상일 때

- for 문 방식과 stream 방식을 둘 다 두고(`_xxxByLoop` / `_xxxByStream`), 실행할 때마다
  `ThreadLocalRandom.current().nextBoolean()` 으로 고른다. 정규식으로 찾아 바꾸는 방식도 같은
  자격으로 취급한다(`_trimLeadingZeroByRegex`).
- **두 구현은 결과가 같아야 한다.** 한쪽만 고치면 테스트가 간헐적으로 깨진다.
- 고친 뒤에는 테스트를 여러 번 돌려 양쪽이 다 실행되게 한다. 새로 넣었으면 리플렉션으로
  두 구현을 맞붙여 비교한다.
- `Stream.peek` 처럼 실행이 보장되지 않는 중간 연산에 결과를 의존하지 않는다. 부수 효과가
  필요하면 종단 연산(`forEach`)을 쓴다.
- `Collectors.toMap` 은 값이 `null` 이면, `Collectors.groupingBy` 는 분류가 `null` 이면 예외를
  던진다. 그런 자리에서는 `forEach` 로 담는다.
- 가변인자 배열을 다른 메서드에 그대로 넘기지 않는다 — javac 이 `[varargs]` heap pollution
  경고를 낸다. `List` 에 옮겨 담아 넘긴다.
- 두 구현이 실패하는 시점도 맞춘다. 한쪽은 부를 때, 다른 쪽은 쓸 때 예외가 나면 무작위 분기가
  관찰 가능해진다.

## 이 저장소의 구성

- `s.util.*` 은 스태틱 유틸리티(비공개 생성자), `s.type.tuple.*` 은 데이터 타입(인스턴스 메서드).
- 문자열 유틸은 `null` 을 `null` 로 돌려주고, 컬렉션 유틸은 `null` 파라미터를 바로 실패시킨다.
  클래스마다 규칙이 다르니 새 메서드를 넣기 전에 그 클래스의 Javadoc 을 먼저 본다.
